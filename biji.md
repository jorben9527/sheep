# 抗动态干扰的三维场景结构化重建方法

### quesion
1) NeRF 是隐式模型，渲染时需要进行体素采样和积分（计算量大）。3DGS 是显式模型（高斯球） (什么是显示，隐式模型) 
显式模型 (Explicit Representation)：直接、明确地描述和存储三维空间的几何形状和属性。
隐式模型 (Implicit Representation)：通过一个函数（通常是神经网络）来间接编码三维空间的属性，需要查询才能得到几何和颜色信息。
2) 体渲染技术 (Volume Rendering) 体渲染将沿着一条光线 $r(t) = \mathbf{o} + t\mathbf{d}$（其中 $\mathbf{o}$ 是相机原点，$\mathbf{d}$ 是光线方向）采样得到的颜色和密度进行积分。
光栅化:
3) 3DGS中的预处理阶段，运动恢复结构（sfm）到点云生成，稠密匹配都是什么意思，怎么实现的。
SfM 是一种从一系列二维图像中自动估计三维结构（即场景几何）和相机运动（即每张照片的相机姿态）的技术。它解决了 “什么结构 (Structure)” 和 “如何运动 (Motion)” 的问题。  
稠密匹配 (Dense Matching) / 稠密重建  
SfM 得到的点云是稀疏的（只有关键点的位置），这对于 NeRF 或 3DGS 的初始化来说通常不够。我们需要更多、更密集的点。
4) 什么是带颜色的稀疏点云和相机姿态,各自在3DGS中起到什么作用。
稀疏点云（少量、高置信度的三维点）:在三维空间中一系列离散的点的集合。这些点是由 SfM 算法通过对多张图像中的少量高置信度的匹配特征点进行三角测量得到的。

相机姿态 (Camera Pose)定义： 描述每张输入图像拍摄时相机在三维世界坐标系中的位置和方向。姿态通常由两部分组成
作用：定义世界的框架： 相机姿态是 3DGS 所有优化和渲染的基础。它决定了场景中所有三维点（包括高斯球体）的位置。


相机内参（如焦距、主点）和相机外参（$\mathbf{R}$ 和 $\mathbf{t}$）共同定义了三维点如何投影到二维图像上。
指导可微分渲染： 在 3DGS 的训练过程中，相机姿态 $\mathbf{R}$ 和 $\mathbf{t}$ 被用来进行：1. 将 3D 高斯投影到 2D 图像平面。2. 计算渲染图像与真实图像之间的损失。
相机姿态 ($\mathbf{R}, \mathbf{t}$) & 内参SfM 的核心输出，确定所有图片的视角。不可或缺！ 用于将 3D 高斯投影到 2D 图像，计算渲染损失（使渲染图像与真实图像匹配）。

相机姿态 告诉 3DGS “我是从哪里看这个场景的”。

稀疏/稠密点云 告诉 3DGS “场景中的物体大致在哪里，它们是什么颜色的”。

5) 怎么理解相机姿态在3D 高斯投影到 2D 图像发挥的作用的
相机姿态的外参 $(\mathbf{R}, \mathbf{t})$ 将世界坐标 $\mathbf{X}_w$ 转换到以相机光心为原点的相机坐标系 $\mathbf{X}_c = [x_c, y_c, z_c]^T$
 $$\mathbf{X}_c = \mathbf{R} \mathbf{X}_w + \mathbf{t}$$
6) 鲁棒性

计算渲染损失（使渲染图像与真实图像匹配）。