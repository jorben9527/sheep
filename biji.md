# 抗动态干扰的三维场景结构化重建方法

### quesion
1) NeRF 是隐式模型，渲染时需要进行体素采样和积分（计算量大）。3DGS 是显式模型（高斯球） (什么是显示，隐式模型) 
显式模型 (Explicit Representation)：直接、明确地描述和存储三维空间的几何形状和属性。
隐式模型 (Implicit Representation)：通过一个函数（通常是神经网络）来间接编码三维空间的属性，需要查询才能得到几何和颜色信息。
2) 体渲染技术 (Volume Rendering) 体渲染将沿着一条光线 $r(t) = \mathbf{o} + t\mathbf{d}$（其中 $\mathbf{o}$ 是相机原点，$\mathbf{d}$ 是光线方向）采样得到的颜色和密度进行积分。
光栅化:
3) 3DGS中的预处理阶段(COLMAP)，
运动恢复结构（sfm）到点云生成，稠密匹配都是什么意思，怎么实现的。
SfM 是一种从一系列二维图像中自动估计三维结构（即场景几何）和相机运动（即每张照片的相机姿态）的技术。它解决了 “什么结构 (Structure)” 和 “如何运动 (Motion)” 的问题。  

稠密匹配 (Dense Matching) / 稠密重建  
SfM 得到的点云是稀疏的（只有关键点的位置），这对于 NeRF 或 3DGS 的初始化来说通常不够。我们需要更多、更密集的点。
稠密匹配利用已知的相机位置和多张图像的纹理相似性，来精确计算场景中每个像素对应的三维位置，生成高质量的稠密点云，为 3DGS 神经优化做好准备。


4) 什么是带颜色的稀疏点云和相机姿态,各自在3DGS中起到什么作用。
稀疏点云（少量、高置信度的三维点）:在三维空间中一系列离散的点的集合。这些点是由 SfM 算法通过对多张图像中的少量高置信度的匹配特征点进行三角测量得到的。

相机姿态 (Camera Pose)定义： 描述每张输入图像拍摄时相机在三维世界坐标系中的位置和方向。姿态通常由两部分组成
作用：定义世界的框架： 相机姿态是 3DGS 所有优化和渲染的基础。它决定了场景中所有三维点（包括高斯球体）的位置。


相机内参（如焦距、主点）和相机外参（R 和 t）共同定义了三维点如何投影到二维图像上。
指导可微分渲染： 在 3DGS 的训练过程中，相机姿态 R和 t被用来进行：1. 将 3D 高斯投影到 2D 图像平面。2. 计算渲染图像与真实图像之间的损失。
相机姿态 R, t)  内参SfM 的核心输出，确定所有图片的视角。不可或缺！ 用于将 3D 高斯投影到 2D 图像，计算渲染损失（使渲染图像与真实图像匹配）。

相机姿态 告诉 3DGS “我是从哪里看这个场景的”。

稀疏/稠密点云 告诉 3DGS “场景中的物体大致在哪里，它们是什么颜色的”。

5) 怎么理解相机姿态在3D 高斯投影到 2D 图像发挥的作用的
   步骤一：世界坐标系 --> 相机坐标系 (使用外参)
相机姿态的外参 (R, t) 将世界坐标 X_w 转换到以相机光心为原点的相机坐标系 X_c = [x_c, y_c, z_c]^T
   X_c = R*X_w + t
   步骤二：相机坐标系 --> 图像坐标系 (使用内参)
   内参 $\mathbf{K}$ 将相机坐标 $\mathbf{X}_c$ 投影到 2D 图像平面上的像素坐标 $\mathbf{p} = [u, v]^T$：透视投影：$$u' = x_c / z_c$$$$v' = y_c / z_c$$（这一步将 3D 点映射到归一化的 2D 平面）
   
   像素缩放和偏移：$$u = f_x u' + c_x$$$$v = f_y v' + c_y$$（这一步使用焦距和主点，将点映射到最终的像素坐标 $(u, v)$ 上）

   3DGS 的特殊之处：投影高斯协方差3DGS 不仅仅是投影一个点，它还要投影整个椭球体的形状。高斯球体在世界坐标系中的协方差矩阵是 $\boldsymbol{\Sigma}$，它投影到 2D 图像平面上的投影协方差 $\boldsymbol{\Sigma}'$ 是一个 $2 \times 2$ 的矩阵。$\boldsymbol{\Sigma}'$ 的计算公式比较复杂，但其核心是利用雅可比矩阵 $\mathbf{J}$（描述 3D 到 2D 投影的局部线性变换）和相机外参 $\mathbf{R}$：$$\boldsymbol{\Sigma}' = \mathbf{J} \mathbf{R} \boldsymbol{\Sigma} \mathbf{R}^T \mathbf{J}^T$$

   总结：相机姿态的核心作用定位 (Location)： 外参 $\mathbf{R}, \mathbf{t}$ 决定了高斯中心 $\boldsymbol{\mu}_i$ 在图像上的像素坐标。塑形 (Shape)： 外参 $\mathbf{R}$ 参与计算高斯在 2D 图像上的投影协方差 $\boldsymbol{\Sigma}'$，决定了高斯被渲染出来时椭圆的大小和方向。如果没有准确的相机姿态，即使高斯球体本身是完美的，投影也会出错，渲染出来的图像将与实际图像错位，导致损失函数无法收敛，3DGS 训练就会失败。
6) 鲁棒性
鲁棒性指的是一个系统、算法或模型在面对各种扰动、错误、不确定性或输入变化时，仍能保持稳定、正确和高性能的能力。
计算渲染损失（使渲染图像与真实图像匹配）。

7)  如何在 DINOv2 + 3DGS 中体现鲁棒性
？您的项目通过结合两种技术，实现了对多重干扰的鲁棒性：
A. DINOv2 提供的特征鲁棒性锚定静态结构： DINOv2 提取的特征具有很高的语义一致性。这意味着，静态物体（如墙壁、地面）在不同视角下的 DINOv2 特征图看起来几乎一致。指导优化： 当您使用 DINOv2 特征损失来约束 3DGS 时，您是在告诉模型：“渲染出的特征图必须和 DINOv2 提取的特征图相似。” 这使得 3DGS 更关注语义和结构，而不是 RGB 颜色的微小变化，从而增加了对光照和视角变化的鲁棒性。
B. 抗动态干扰（核心创新）这是最直接的鲁棒性体现。问题： 动态物体在某一帧的图像中是有颜色的，3DGS 会尝试用高斯球体去拟合它。解决方案（例）： 特征不一致惩罚。一个静态高斯 $G_s$ 在所有帧中都存在，它的 DINOv2 特征在不同帧之间是一致的。一个动态高斯 $G_d$ 只在第 $k$ 帧中存在。它在第 $k$ 帧对损失贡献大，但在其他帧中没有贡献。通过设计一个损失或正则项，来惩罚那些跨帧贡献不一致或特征一致性低的高斯。这样，动态高斯 $G_d$ 的不透明度 $\alpha$ 将在优化中被迫衰减至零，从而鲁棒地去除动态干扰产生的“鬼影”。

8) 高斯是怎么形成的
A. 初始形成：基于点云
3DGS 的初始高斯形成是基于 SfM/MVS 预处理阶段生成的稠密点云。
1.起点：点云点
-稠密点云中的每一个点 $P(x, y, z, \text{RGB})$ 都会被作为初始高斯的中心。
2.设定参数：
-位置 $(\boldsymbol{\mu})$： 设为点 $P$ 的坐标 $(x, y, z)$。
-颜色 $(\mathbf{c})$： 设为点 $P$ 的 RGB 颜色。
-不透明度 $(\alpha)$： 设为一个较小的初始值（例如 $0.01$）。
-协方差 $(\boldsymbol{\Sigma})$： 用来定义高斯的形状和大小。
  -初始尺寸： 通常通过计算点 $P$ 到其最近的 $k$ 个邻居点（例如 $k=3$ 或 $k=5$）的平均距离 $d_{\text{avg}}$ 来估计。
  -初始协方差： 设为一个各向同性的矩阵 $s^2 \mathbf{I}$，其中 $s$ 正比于 $d_{\text{avg}}$。这使得初始高斯都是小球体。
  协方差
     是一个 $3 \times 3$ 矩阵
     决定了椭球体的三个主轴的长度和空间方向。$$\boldsymbol{\Sigma} = \mathbf{R} \boldsymbol{S} \mathbf{R}^T$$
     $\mathbf{S}$ (尺度/尺寸)三个特征值 $\lambda_1, \lambda_2, \lambda_3$ 构成的对角矩阵。$\lambda_i$ 的平方根 $\sqrt{\lambda_i}$ 决定了椭球体沿三个主轴的尺寸（半轴长）。这些值决定了高斯的大小。$\mathbf{R}$ (旋转/方向)三个特征向量构成的旋转矩阵。决定了椭球体的空间方向（即三个主轴指向哪里）。它决定了高斯的形状和方向。

总结： 初始高斯是稠密点云的**“膨胀版”**，它提供了一个良好的几何起点。

B. 优化与细化：梯度驱动
在初始高斯集合 $S$ 形成后，它们通过梯度下降和自适应控制来学习场景的细节：
1.学习位置、颜色和形状： 通过最小化渲染损失（如 $L_{\text{RGB}}, L_{\text{SSIM}}, L_{\text{DINOv2}}$），高斯球体的参数 $\boldsymbol{\mu}, \mathbf{c}, \boldsymbol{\Sigma}, \alpha$ 会被调整，使它们投影渲染出的图像尽可能匹配真实的图像。
2.自适应控制（分裂与复制）：
  分裂 (Splitting)： 当一个高斯位于纹理复杂或重建误差高的区域时，它会被复制成两个更小的高斯，以捕捉更精细的细节。
  裁剪 (Pruning)： 当一个高斯的不透明度 $\alpha$ 变得非常低时，或者对图像的贡献极小时，它会被移除（剪枝）。

9) 动态高斯和静态高斯是怎么一回事
“动态高斯”和“静态高斯”并不是 3DGS 原始论文中定义的参数类型，而是您项目中为了实现“抗动态干扰”而在概念上或技术上进行区分的高斯子集。

## 3DGS训练阶段

1. 训练阶段的输入与输出
   输入 (Input)	: 1. 原始训练图像集。2. 固定的相机姿态（SfM/MVS 预处理输出）。3. 初始高斯集合（稠密点云初始化）。
   可学习参数:  每个高斯 $G_i$ 的参数：位置 $\boldsymbol{\mu}_i$、颜色 $\mathbf{c}_i$（SH 系数）、不透明度 $\alpha_i$、尺度 $s_i$ 和旋转 $q_i$。（您的项目还包括 DINOv2 特征 $\mathbf{f}_i$）。
   输出 (Output)	一组经过优化的高斯参数，能够从任意新视角快速渲染出高质量的图像。

2. 核心迭代步骤
  训练是一个循环过程，通常迭代数万次：
  步骤 A: 
  随机采样与渲染 (Forward Pass)随机采样相机： 从训练图像集中随机选取一张图片，并加载其对应的固定相机姿态 $\mathbf{R}_{\text{cam}}, \mathbf{t}_{\text{cam}}$。
  可微分渲染：
  使用选定的相机姿态，将所有 3D 高斯投影到 2D 图像平面。
  按照深度进行排序（从后到前）。
  执行高效的 $\alpha$ 混合（Splatting），渲染出当前视角的图像 $C_{\text{render}}$。
  引入创新点： 您的项目在此处不仅渲染 $C_{\text{render}}$（RGB 图像），还要渲染出 特征图 $F_{\text{render}}$，这是由高斯绑定的 DINOv2 特征 $\mathbf{f}_i$ 混合而成的。
  步骤 B: 
  损失计算 (Loss Calculation)
    1.核心损失 (RGB)： 比较渲染图像 $C_{\text{render}}$ 与真实图像 $C_{\text{gt}}$。
    $$L_{\text{RGB}} = \lambda_1 L_1(C_{\text{render}}, C_{\text{gt}}) + \lambda_2 L_{\text{D-SSIM}}(C_{\text{render}}, C_{\text{gt}})$$
    （通常结合 $L_1$ 损失和 SSIM 损失，其中 D-SSIM 是 3DGS 提出的更适用于深度学习的 SSIM 变体。）
    创新点损失（DINOv2 特征损失）： 这是您项目的关键！
    首先，使用预训练好的 DINOv2 模型从 $C_{\text{gt}}$ 中提取地面真值特征图 $F_{\text{gt}}$。然后，计算渲染特征图 $F_{\text{render}}$ 与 $F_{\text{gt}}$ 之间的差异。
      $$L_{\text{DINOv2}} = \lambda_3 L_1(F_{\text{render}}, F_{\text{gt}})$$

      目标： 这个损失迫使每个高斯球体 $G_i$ 学习到一个能准确重现 DINOv2 语义信息的特征向量 $\mathbf{f}_i$，保证了高斯对底层语义结构的拟合。

    3.总损失：

    $$L_{\text{total}} = L_{\text{RGB}} + L_{\text{DINOv2}} + L_{\text{Regularization}}$$

  步骤 C:
  反向传播与优化 (Backward Pass & Update)梯度计算：
   通过可微分渲染，计算总损失 $L_{\text{total}}$ 对所有可学习参数（$\boldsymbol{\mu}, \mathbf{c}, \boldsymbol{\Sigma}, \alpha, \mathbf{f}$ 等）的梯度。参数更新： 使用优化器（如 Adam）沿着梯度方向更新所有高斯参数。
  步骤 D:
   自适应控制（高斯管理）
   每隔固定的迭代次数（如 100 迭代），执行高斯集的管理，以防止过拟合并提高效率：透明度剪枝 (Pruning)： 移除不透明度 $\alpha$ 极低的高斯，以减少计算量。高斯密度控制 (Cloning & Splitting)：对于渲染误差大的区域：高斯被分裂成两个更小的高斯，以捕捉细节。对于尺寸过大的区域：高斯被复制，并收缩尺度，以提高分辨率。

  ### 
  可微分渲染
    可微分渲染的目标是：

将渲染过程变成一个可导的函数。 这样，我们可以利用链式法则（反向传播）来计算损失函数对三维场景参数（如高斯的位置、形状、颜色）的梯度，从而指导这些三维参数的优化。


## 项目创新点

（一）引入DINOv2特征进行干扰区分

（二）3DGS算法下的损失函数再设计

（三）基于高斯椭球分布关系的重建后处理
我作为一名零基础的大学生即将面临选题为基于DINOv2和3DGS的抗动态干扰的三维场景结构化重建方法的汇报答辩，我想仔细了解该方面的各个知识，实现项目的各个细节都要涉及，来龙去脉都需要弄清楚，以便进行答辩汇报